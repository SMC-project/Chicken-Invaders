#include <iostream>

int main()
{
	GameLoop();
	system("pause");
}

void GameLoop()
{
	std::pair<int, int> newPlayerPos;
	std::pair<int, int> playerPos(1, 2);
	bool stopGame = false;

	while (stopGame == false)
	{
		
		//Sleep(500);
	}
}